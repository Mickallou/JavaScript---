const age = 44;// Implicity defines type as number
const age2: number = 88;// Explicitly defines type as number
let age3: number;
age3 = 99;

alert(age + age2 + age3);

console.log(`
Hello World! ${age} ${age2} ${age3}

`);
